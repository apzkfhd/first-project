// const backTitle = document.querySelector(".service-section .back-title");
// window.addEventListener("scroll", () => {
//   // console.log(scrollY);
//   if (scrollY > 4780 && scrollY < 7724) {
//     backTitle.classList.add("sticky");
//   } else {
//     backTitle.classList.remove("sticky");
//   }
// });
// const section = document.querySelector(".service-section");
// const backTitle = document.querySelector(".service-section .back-title");

// window.addEventListener("scroll", () => {
//   const scrollY = window.scrollY;

//   const sectionTop = section.offsetTop;
//   const sectionHeight = section.offsetHeight;
//   const titleHeight = backTitle.offsetHeight;

//   // 고정 시작 지점
//   const start = sectionTop;

//   // 고정 종료 지점 (텍스트가 섹션 끝에 닿기 직전)
//   const end = sectionTop + sectionHeight - titleHeight;

//   if (scrollY >= start && scrollY <= end) {
//     backTitle.classList.add("sticky");
//   } else {
//     backTitle.classList.remove("sticky");
//   }
// });
